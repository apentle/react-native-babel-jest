language: node_js
node_js:
  - "4.2"

install:
  - npm install --save react-native invariant
  - npm install --save-dev coveralls
  - npm install

script:
  - npm test

after_script:
  - cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js

branches:
  only:
    - develop
